---
name: CI - Lint

on:
  workflow_call:
    inputs:
      enable_actionlint:
        description: 'Enable actionlint'
        required: false
        type: boolean
        default: true
      enable_shellcheck:
        description: 'Enable shellcheck'
        required: false
        type: boolean
        default: false
      enable_yamllint:
        description: 'Enable yamllint'
        required: false
        type: boolean
        default: true
      yamllint_config:
        description: 'Path to yamllint config file'
        required: false
        type: string
        default: '.yamllint.yml'
      yamllint_strict:
        description: 'Run yamllint in strict mode'
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  actionlint:
    name: Action Lint
    runs-on: ubuntu-latest
    if: ${{ inputs.enable_actionlint }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Run actionlint
        uses: reviewdog/action-actionlint@50842263c20a7c46bd0065b9e624d3c569db061e # v1
        with:
          reporter: github-pr-review
          fail_level: error

  shellcheck:
    name: Shell Check
    runs-on: ubuntu-latest
    if: ${{ inputs.enable_shellcheck }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Run shellcheck
        uses: reviewdog/action-shellcheck@0722bbdb0d47f04c1b53b8734d2422ac63a45ec6 # v1
        with:
          reporter: github-pr-review
          fail_level: error

  yamllint:
    name: YAML Lint
    runs-on: ubuntu-latest
    if: ${{ inputs.enable_yamllint }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install yamllint
        run: pip install yamllint

      - name: Run yamllint
        env:
          YAMLLINT_STRICT: ${{ inputs.yamllint_strict }}
          YAMLLINT_CONFIG: ${{ inputs.yamllint_config }}
        run: |
          set -euo pipefail
          # Values arrive via env: and are read as shell variables, not
          # substituted into the body by the workflow parser — a caller value
          # containing $(...) stays a config path instead of becoming code.
          ARGS=()
          if [ "$YAMLLINT_STRICT" == "true" ]; then
            ARGS+=("--strict")
          fi
          yamllint -c "$YAMLLINT_CONFIG" "${ARGS[@]}" .
